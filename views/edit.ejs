<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
     
    <div class="w-full h-[750px] bg-zinc-900 text-white p-10">
        <div class="nav">
            <a class="mb-8 text-blue-400"  href="/read">
                Back to users ->
            </a>
        </div>
        <form action="/update/<%= user._id%>" class="" method="post">

            <h1 class="text-3xl tracking-tighter mb-10 pl-2">Edit User</h1>
            <input type="text" placeholder="name"  name = "name"  class="px-5 py-3 bg-transparent border-2 border-zinc-700 rounded-lg py-2 outline-none ml-2" value="<%= user.name %>">
            <input type="text" placeholder="email"  name = "email" class="px-5 py-3 bg-transparent border-2 border-zinc-700 rounded-lg py-2 outline-none ml-2"  value="<%= user.email %>">
            <input type="text" placeholder="image_url" name = "image_url"  class="px-5 py-3 bg-transparent border-2 border-zinc-700 rounded-lg py-2 outline-none m-2"  value="<%= user.image_url %>">
            <input class="px-5 py-2 bg-yellow-600 rounded-lg outline-none" type="submit" value="Update user">
        </form>
    </div>
    <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>