<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
     
    <div class="w-full h-full bg-zinc-900 text-white p-10 ">

        <div class="nav">
            <a class="mb-8 text-blue-400"  href="/">
                Back to home ->
            </a>
        </div>

       <h1 class="text-3xl tracking-tighter mb-8 ml-2">All Users</h1> 



       <div class="users flex gap-3 flex-wrap">


        <%if(users.length >0) { %>
 <% users.forEach((user)=>{ %>
 

    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
        <div class="w-full h-52 rounded-lg bg-zinc-700">
           <img class="w-full h-full bg-cover " src="<%= user.image_url %>" alt="">
        </div>
        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name%></h3>
        <h5 class="text-blue-400"><%= user.email%></h5>

        <div class="flex gap-3">
           <a class="text-blue-600" href="/edit/<%= user._id %>">Edit user</a>
           <a class="text-red-700" href="/delete/<%= user._id %>">Delete user</a>
        </div>
       </div>

     
  <% }) %> 
      
       </div>

       <% } else { %> 
       <h3 class="text-red-700">no users yet</h3>

       <% } %> 
       
        
    </div>
    <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>